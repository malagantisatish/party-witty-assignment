# React + TypeScript + Vite

# Initially, I used the command npm create vite@latest party-witty-assignment to start the React application.

# After setup, I removed the existing boilerplate code and unused assets.

# Then, I installed Tailwind CSS using the command npm install tailwindcss @tailwindcss/vite.

# I changed the default development port from 5173 to 3000.

# Tailwind CSS was configured and integrated into the project.

# I began by downloading the required assets and building the Sidebar, writing the code in a structured way for better readability and scalability.

# I created a global reusable Modal component so that any content can be passed as children when needed.

# The project is built using TypeScript to improve type safety and error handling.
